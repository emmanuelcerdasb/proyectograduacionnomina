@model ProyectoGraduacionNomina.Credencial

@{
    ViewBag.Title = "Crear Credencial";
}

<div class="form-card">
    <div class="form-header">
        <h2><i class="bi bi-person-plus me-2"></i>Crear Credencial</h2>
    </div>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="form-body">
            @Html.ValidationSummary(true, "", new { @class = "text-danger mb-3" })

            <div class="mb-3">
                @Html.LabelFor(m => m.usuario,"Usuario", new { @class = "form-label fw-semibold" })
                @Html.EditorFor(m => m.usuario, new { htmlAttributes = new { @class = "form-control modern-input", placeholder = "Ingrese el nombre de usuario" } })
                @Html.ValidationMessageFor(m => m.usuario, "", new { @class = "text-danger small" })
            </div>

            <div class="mb-3">
                @Html.LabelFor(m => m.contrasena,"Contraseña", new { @class = "form-label fw-semibold" })
                @Html.PasswordFor(m => m.contrasena, new { @class = "form-control modern-input", placeholder = "Contraseña temporal" })
                @Html.ValidationMessageFor(m => m.contrasena, "", new { @class = "text-danger small" })
            </div>

            <div class="mb-3">
                @Html.Label("Rol", new { @class = "form-label fw-semibold" })
                @Html.DropDownList("Rol_idRol", null, "-- Seleccione un rol --", new { @class = "form-select modern-input" })
                @Html.ValidationMessageFor(m => m.Rol_idRol, "", new { @class = "text-danger small" })
            </div>

            <div class="form-check mb-3">
                @Html.CheckBoxFor(m => m.activo, new { @class = "form-check-input" })
                @Html.LabelFor(m => m.activo, "Estado Activo", new { @class = "form-check-label" })
            </div>
        </div>

        <div class="form-footer">
            <a href="@Url.Action("Index")" class="btn btn-light cancel-btn">
                <i class="bi bi-arrow-left-circle me-1"></i> Volver
            </a>
            <button type="submit" class="btn btn-primary-custom">
                <i class="bi bi-save2 me-1"></i> Crear
            </button>
        </div>
    }
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
