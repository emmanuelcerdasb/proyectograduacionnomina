@model ProyectoGraduacionNomina.Rol

@{
    ViewBag.Title = "Crear Rol";
}

<div class="form-card">
    <div class="form-header">
        <h2><i class="bi bi-person-badge me-2"></i>Crear Rol</h2>
    </div>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="form-body">
            @Html.ValidationSummary(true, "", new { @class = "text-danger mb-3" })

            <div class="mb-3">
                @Html.LabelFor(m => m.nombre,"Nombre", new { @class = "form-label fw-semibold" })
                @Html.EditorFor(m => m.nombre, new { htmlAttributes = new { @class = "form-control modern-input", placeholder = "Nombre del rol" } })
                @Html.ValidationMessageFor(m => m.nombre, "", new { @class = "text-danger small" })
            </div>

            <div class="mb-3">
                @Html.LabelFor(m => m.descripcion,"Descripción", new { @class = "form-label fw-semibold" })
                @Html.TextAreaFor(m => m.descripcion, new { @class = "form-control modern-input", rows = 3, placeholder = "Descripción (opcional)" })
                @Html.ValidationMessageFor(m => m.descripcion, "", new { @class = "text-danger small" })
            </div>
        </div>

        <div class="form-footer">
            <a href="@Url.Action("Index")" class="btn btn-light cancel-btn">
                <i class="bi bi-arrow-left-circle me-1"></i> Volver
            </a>
            <button type="submit" class="btn btn-primary-custom">
                <i class="bi bi-save2 me-1"></i> Crear
            </button>
        </div>
    }
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
